# ~/.ssh/config    -*- conf -*-

ControlMaster auto
ControlPath /tmp/%r@%h:%p
Compression yes
ServerAliveInterval 5
EscapeChar none

Include config-forges

Host *.pagekite.me
  CheckHostIP no
  ProxyCommand /usr/bin/corkscrew %h 443 %h %p

# Include config-${LOCATION}
